pipeline {
    agent any
    tools { 
        maven 'Jenkins Maven' 
    }
    stages {
        stage('CI') {
            steps { echo 'CI' }  
        }
        stage('UAT deploy') {
            steps { echo 'uat deploy' }
        }
        stage('UAT test') {
            steps { echo 'uat test' }
        }
        stage('PROD') {
            steps { echo 'prod' }
        }
    }
}
